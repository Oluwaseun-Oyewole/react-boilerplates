export const enum Routes {
  base = "/",

  //auth
  auth = "/auth",
  login = "/auth/login",
  signUp = "/auth/register",

  dashboard = "/dashboard",
  products = "/dashboard/products",
  albums = "/dashboard/albums",
}
